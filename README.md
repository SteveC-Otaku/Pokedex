# 宝可梦图鉴 Pokédex

一个功能完整的宝可梦图鉴应用，使用 Next.js 构建，支持中英文双语。

## ✨ 功能特性

- 📚 **完整的宝可梦数据**：包含所有世代的宝可梦信息
- 🔍 **强大的搜索功能**：支持按名称、编号搜索
- 🎯 **多维度筛选**：
  - 按世代/地区筛选
  - 按属性筛选（支持多选，筛选拥有所有选中属性的宝可梦）
  - 按种族值排序（单项或总和）
- 📊 **详细信息展示**：
  - 基础信息（属性、身高、体重等）
  - 种族值可视化
  - 属性相克关系
  - 招式列表
  - 出现地点（按世代分组）
  - 进化链
- 🎲 **捕获率计算器**：
  - 支持多种精灵球
  - 支持异常状态加成
  - 实时计算捕获概率
- 🌐 **多语言支持**：中文/英文切换
- 🎨 **现代化 UI**：响应式设计，支持深色模式

## 🌐 在线访问

**GitHub Pages**: https://stevec-otaku.github.io/Pokedex/

## 🚀 快速开始

### 环境要求

- Node.js 18+ 
- pnpm (推荐) 或 npm

### 安装依赖

```bash
pnpm install
```

### 开发模式

```bash
pnpm dev
```

访问 [http://localhost:3000](http://localhost:3000)

### 构建生产版本

```bash
pnpm build
```

构建后的静态文件在 `out/` 目录。

### 生成数据

项目需要先生成宝可梦数据文件：

```bash
# 生成基础列表
pnpm generate-list

# 生成完整数据（包含详细信息、招式、出现地点等）
pnpm generate-full-data

# 更新出现地点数据（从 PokeAPI）
pnpm update-locations

# 更新出现地点数据（从中文 Wiki，仅限 Gen 1-7）
pnpm update-locations-wiki
```

详细的数据生成说明请查看 [README-DATA.md](./README-DATA.md)

### 打包分发

```bash
pnpm package
```

会在 `dist/Pokedex` 目录生成可分发版本，包含启动脚本。

## 📁 项目结构

```
Pokedex/
├── app/                    # Next.js App Router
│   ├── page.tsx           # 主页面
│   ├── layout.tsx         # 根布局
│   └── globals.css        # 全局样式
├── components/            # React 组件
│   ├── pokemon-*.tsx      # 宝可梦相关组件
│   └── ui/                # UI 组件库
├── lib/                   # 工具函数
│   ├── pokemon-api.ts     # PokeAPI 接口
│   ├── pokemon-types.ts   # TypeScript 类型定义
│   ├── pokemon-utils.ts   # 工具函数（捕获率计算等）
│   └── utils.ts           # 通用工具
├── scripts/               # 数据生成脚本
│   ├── generate-*.ts      # 数据生成脚本
│   └── package-*.ts       # 打包脚本
├── public/                # 静态资源
│   └── data/              # 数据文件（需生成）
└── contexts/              # React Context
    └── language-context.tsx # 语言切换
```

## 🛠️ 技术栈

- **框架**: Next.js 16 (App Router)
- **语言**: TypeScript
- **样式**: Tailwind CSS
- **UI 组件**: shadcn/ui
- **数据获取**: SWR
- **数据源**: 
  - [PokeAPI](https://pokeapi.co/)
  - [宝可梦中文 Wiki](https://wiki.52poke.com/)

## 📝 数据说明

项目使用本地 JSON 文件存储宝可梦数据，包括：

- `pokemon-list.json`: 基础列表（编号、名称、图片等）
- `pokemon-full-data.json`: 完整数据（包含详细信息、招式、出现地点等）

数据文件已包含在仓库中（约 35MB），可直接使用。如果需要重新生成，请运行数据生成脚本。

## 🌐 GitHub Pages 部署

### 自动部署（已配置）

项目已配置 GitHub Actions 自动部署：

1. 在 GitHub 仓库设置中，进入 **Settings > Pages**
2. 选择 **Source**: `GitHub Actions`
3. 推送代码到 `main` 分支后，会自动构建并部署

部署地址：`https://stevec-otaku.github.io/Pokedex/`

### 手动部署

如果需要手动部署：

```bash
# 构建静态文件（会自动使用 /Pokedex basePath）
GITHUB_PAGES=true pnpm build

# 构建后的文件在 out/ 目录
# 将 out/ 目录的内容推送到 gh-pages 分支
```

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 📄 许可证

MIT License

## 🙏 致谢

- [PokeAPI](https://pokeapi.co/) - 提供宝可梦数据 API
- [宝可梦中文 Wiki](https://wiki.52poke.com/) - 提供中文数据
- [shadcn/ui](https://ui.shadcn.com/) - UI 组件库
